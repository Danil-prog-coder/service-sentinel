# Copy to .env and fill in. Never commit .env.
# On a hosting platform set these in its environment variables panel instead.

# Token from @BotFather
TELEGRAM_BOT_TOKEN=
# Chat that receives alerts and may control the bot (see `python -m monitor --telegram-chats`)
TELEGRAM_CHAT_ID=

# Optional
LOG_LEVEL=INFO
