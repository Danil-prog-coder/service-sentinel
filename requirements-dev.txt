# Development dependencies (tests and static checks).
# Mirrors [project.optional-dependencies] dev in pyproject.toml.
#
#   pip install -r requirements-dev.txt
#
# The project itself still has to be installed: pip install -e .

-r requirements.txt

pytest>=8
pytest-asyncio>=0.24
ruff>=0.6
mypy>=1.11
types-PyYAML
